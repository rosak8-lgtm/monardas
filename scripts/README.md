# Verification

Run `npm run lint` and `npm run build`. Browser checks use Playwright against a running local server and cover the six requested widths, every public route, navigation, form validation, calculator edge cases and reduced motion. Browser tooling is a development-only dependency.
