# Implementation plan and audit

The existing Next.js 15 App Router project contains six commercial routes, a calculator, and a validated development contact endpoint. No static prototypes, assets, ecommerce routes, checkout integration, or HypeOK references are present. Dependencies were not installed at audit time, so an initial build cannot run before installation.

Preserve recovery route URLs, useful recovery copy, calculator inputs, and contact validation. Replace the green visual system and illustrative dashboard with editorial typography and custom structural SVG. Keep primary content server rendered; isolate navigation, calculator, and form interactivity.

Implement shared navigation and footer; corporate homepage; AI offer; Systems, Ventures, Capital; Commerce and Nature; Strategy and Founder. Add metadata, sitemap, robots and honest legal placeholders. Then verify build, lint, routes, responsive layouts, navigation, calculation and form behavior.

Deployment gate: this directory does not contain the existing botanical store. Do not replace a store deployment until its indexed product URLs, cart, checkout and domain routing have been inventoried and preserved. No product redirects are introduced.
